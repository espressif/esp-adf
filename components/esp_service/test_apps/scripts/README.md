# PC MCP client scripts (DUT)

Run from `test_apps/` (parent of `scripts/`). See [`../README.md`](../README.md) for Kconfig options (`ESP_SVC_TEST_*_HOLD_AFTER_SMOKE`, transport enables) and full layout. Scripts: `test_mcp_http.py`, `test_mcp_sse.py`, `test_mcp_ws.py`, `test_mcp_uart.py`.
